spring:
  application:
    name: cadastro-service
  cloud:
    consul:
      host: consul
      port: 8500
      config:
        enabled: true
        prefix: config
        defaultContext: apps
        profileSeparator: '::'